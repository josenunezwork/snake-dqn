# Native pretrap counterfactual: closed with an unaudited result

**Status: `CLOSED_INVALID_STOP_AUDIT_FAILED_NATIVE_RESULTS_PROVISIONAL`.** Qualification, alternative probes, and saved-output analysis finished, but the required independent audit failed before its first raw-file read. The frozen protocol allowed no recovery, so there was no correction run, suffix audit, or retry. The numerical findings below are **provisional and must not be treated as audited evidence**. The [closeout](</Users/josenunez/Projects/ml/snake-dqn-artifacts/ongoing-research-20260913/apex-native-pretrap-counterfactual/closeout.json>) is the authority for this status.

The study selected 15 previously identified fatal endpoints across three seeds and both arms. All 15 factual two-frame reconstructions qualified in the completed stage report. Fourteen cases had comparable safe normal alternatives; one had none and received no intervention. The saved alternative report and analysis say that all 20 fixed alternatives survived a **two-action local program**: inject the alternative normal action, then, if a safe normal successor exists, inject the lowest-index such action. This yielded at least one recorded two-frame local rescue in each of the 14 comparable cases. These are saved outputs, **not independently verified findings**. The [provisional table](provisional-table.md) preserves the original analysis branch label and the six-cell/three-seed breakdown byte-for-byte; the [provisional representatives](provisional-representatives.json) preserve its example branches byte-for-byte.

| Seed | h64: targets / alternatives / cases rescued | h256: targets / alternatives / cases rescued |
|---|---:|---:|
| 2026101001 | 2 / 2 / 2 | 4 / 3 / 3 |
| 2026101002 | 1 / 2 / 1 | 3 / 5 / 3 |
| 2026101003 | 2 / 3 / 2 | 3 / 5 / 3 |

The intervention does **not** test whether changing only the first action and then following the learned policy would rescue an episode. It does not establish improved learned behavior, later food collection, full-episode survival, fresh-world generalization, or promotion. The original Python RNG state was not saved; the probe used its sealed local seed law and did not restore future food, rewards, or full successor observations from the interrupted episode. These are selected failures, not independent training replications or successful controls. No learning curve was generated. The earlier audited [food-accrual](../apex_horizon_continuation_2026-09-23/food-accrual.png), [learning curves](../apex_horizon_continuation_2026-09-23/learning-curves.png), and [gameplay paths](../apex_horizon_continuation_2026-09-23/representative-paths.png) remain the behavioral evidence for the learned checkpoints; they do not validate this later local probe.

The failure was a logging call in the frozen auditor: `pulse(heartbeat, "audit_raw", path=str(path))` supplied `path` both positionally and by keyword. Python raised `TypeError` before the next line's raw `read_bytes()`. The audit report's `raw_files_attempted=1` was incremented *before* that call, so it records an announced attempt rather than a physical read; `raw_files_parsed=0` and `raw_extracted_frames=0`. Zero audit raw reads is inferred from the frozen source and failure trace, not from direct I/O instrumentation. The preceding nonraw authentication checks returned by control flow, but there is no separate durable authentication-success seal. See the [static failure attestation](</Users/josenunez/Projects/ml/snake-dqn-artifacts/ongoing-research-20260913/apex-native-pretrap-counterfactual/failure-attestation.json>). Administrative receipt reconciliation does not substitute for the missing scientific audit.

The completed stage reports recorded 30 factual and 40 alternative native frame attempts, all finished: 70 total across 15 factual and 20 alternative branches. They recorded zero checkpoint loads, model forwards, optimizer updates, and whole-game replays. The four supervised stages consumed **4.404146126 seconds** of guarded time. The supervisor's sampled peak child RSS was **1.449 GiB** and sampled minimum available RAM was **35.139 GiB**; fast stages can finish between samples. The audit made zero native frame attempts. The closed protocol specified zero recovery and the controller recorded `retry_authorized=false`.

Provenance: reviewed design v3 SHA-256 `175dbcb4f881b600ce9ff08415fd42d4f2f322edbdc1e54b606343a888113ce7`; frozen intent `c5d99917fb44c61f3d096f44be155b9db6833368f37ace969faaa9b63e105752`; input freeze `933fe417198360364f6f3b0c1ca15962a8117ac4f826bd9381f2a4d81d883ca0`; copied table `b9607e40508a6b0c86f66f340fe572c9853fd24a6a2885af57aea9ea2f6f5f54`; copied representatives `4fa45580180fc8e93c66c36c73f435bda06ef8294f9f4781f9973f5c8d3ccdcf`; failure attestation `a63571ba2dce0f1d5460dfc27862b8db0165bdfe72e6375984c1d742f3583899`; closeout `e66e79f205a51d4a48eaed2eee9f052359cf28d86cf68239940d83f46c4be991`. The earlier preparation-access incident remains disclosed in the closeout; this attempt makes no analyst-blindness or all-history novelty claim.
