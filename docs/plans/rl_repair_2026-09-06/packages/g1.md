# G1 — Requalify the PPO-integrated source

**Status:** PLANNED. **Earliest wave:** 8. **Role:** verifier. **Kind:** verification.

**Dependencies:** L1, G0

Produce a new source-specific readiness receipt after L1 changes shared inference, frontend and evaluator code.

Read [decisions and wave plan](../README.md), [agent runbook](../agent_runbook.md), and [dispatch.json](../dispatch.json) before starting. You are not alone: edit only this package's claimed files and inherit accepted dependency commits.

## Exclusive write ownership

- `docs/experiments/ppo_integration_qualification_2026-09-06/` — new

## Implementation / execution steps

1. Freeze the post-L1 integration SHA. Repeat the changed inference/serving/engine/promotion test union, frontend test/build, changed-file lint and the full non-slow suite alone; include the required runtime/slow parity cases.
2. Run candidate-head-specific CPU smoke and serving identity/mask/label checks for both DQN and PPO. Prior G0 receipts do not qualify the changed evaluator or PPO head.
3. Run one 100k hero-transition MPS operability shakedown per algorithm serially, each at most 10 minutes under the runbook caps. Freeze comparison settings before these runs and use outcomes for numerics/cost only. X2 consumes these shakedown receipts rather than duplicating them.
4. Record the new source/profile/contract/checkpoint hashes and invalidate any older serving/evaluator receipt that no longer matches.

## Acceptance evidence

- All changed and retained contracts pass on one post-L1 SHA; no old G0 test count is represented as current proof.
- Both algorithms produce finite, loadable checkpoints and actual serving/evaluation receipts under the same task contract. Resource incidents stop requalification and preserve artifacts.

Use the exact frozen commands from the execution protocol once the dependency entrypoints exist. Do not invent an already-available CLI flag or run a long command during implementation.

## Compatibility and rollback

Keep the already-qualified PQN branch if optional PPO integration fails. Reopen the failing owner and repeat invalidated gates.

## Handoff

Write command/result logs, source/config/checkpoint hashes, changed files, commit SHA, unresolved risks and interface changes to `<EVIDENCE_ROOT>/packages/g1/`. The root resolves EVIDENCE_ROOT to an absolute durable path outside disposable worktrees before dispatch. Obtain a root-issued test/compute slot before running commands. Return absolute artifact paths and hashes; root verifies the durable manifest before accepting the dependency or cleaning a worktree. A passing narrow test does not waive an upstream gate or promote a model.

Audit source: [expanded state/system review](../../../state_and_system_review_2026-09-06.md).
