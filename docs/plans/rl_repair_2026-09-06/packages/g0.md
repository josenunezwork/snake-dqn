# G0 — Qualify the integrated repair

**Status:** PLANNED. **Earliest wave:** 5. **Role:** verifier. **Kind:** verification.

**Dependencies:** P2, A2, E1, E2, H0, OFF, S1

Produce one reviewed implementation-readiness receipt before substantive experiments.

Read [decisions and wave plan](../README.md), [agent runbook](../agent_runbook.md), and [dispatch.json](../dispatch.json) before starting. You are not alone: edit only this package's claimed files and inherit accepted dependency commits.

## Exclusive write ownership

- `docs/experiments/rl_repair_qualification_2026-09-06/` — new

## Implementation / execution steps

1. On one frozen integration SHA, run the affected focused union, full non-slow Python suite with -n2 at most, changed-file formatting/lint and required slow multi-reset/engine parity cases. Run the full suite alone.
2. Run frontend test/build when serving payload/type consumers change. Reproduce actual human-plus-AI runtime behavior headlessly; use browser inspection if visible UI behavior changed.
3. Run tiny CPU PQN and Apex lifecycle smokes in separate disposable paths, including failure cleanup. Then one 100k-hero MPS PQN shakedown, at most 10 minutes, 2 native threads and the runbook memory caps.
4. Check successful and failed checkpoint recovery, all input hashes, actual useful-row accounting and zero leaked children. Use the shakedown only for operability/cost calibration; do not select hyperparameters from its score.

## Acceptance evidence

- All required checks pass on the same integration source identity. No stale test count from the old1943-pass source is treated as current evidence.
- No known contract counterexample remains unresolved in the new profile; legacy fixtures still load/act through legacy adapters.
- Numerical/resource failure halts qualification and preserves artifacts. Readiness does not imply learned skill or statistical superiority.

Use the exact frozen commands from the execution protocol once the dependency entrypoints exist. Do not invent an already-available CLI flag or run a long command during implementation.

## Compatibility and rollback

Failed qualification leaves default serving/champion unchanged; reopen only the failing owner package and repeat invalidated gates.

## Handoff

Write command/result logs, source/config/checkpoint hashes, changed files, commit SHA, unresolved risks and interface changes to `<EVIDENCE_ROOT>/packages/g0/`. The root resolves EVIDENCE_ROOT to an absolute durable path outside disposable worktrees before dispatch. Obtain a root-issued test/compute slot before running commands. Return absolute artifact paths and hashes; root verifies the durable manifest before accepting the dependency or cleaning a worktree. A passing narrow test does not waive an upstream gate or promote a model.

Audit source: [expanded state/system review](../../../state_and_system_review_2026-09-06.md).
