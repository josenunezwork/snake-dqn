# H0 — Build corrected-PQN diagnostic harness

**Status:** PLANNED. **Earliest wave:** 4. **Role:** builder. **Kind:** implementation.

**Dependencies:** P2, E1, S1

Run new protocols without modifying the completed sampler campaign harness.

Read [decisions and wave plan](../README.md), [agent runbook](../agent_runbook.md), and [dispatch.json](../dispatch.json) before starting. You are not alone: edit only this package's claimed files and inherit accepted dependency commits.

## Exclusive write ownership

- `src/scripts/pqn_correctness_diagnostic.py` — new
- `tests/test_pqn_correctness_diagnostic.py` — new

## Implementation / execution steps

1. Build a manifest-first runner around corrected PQN and the new evaluator. Persist recipe, source/config/checkpoint/opponent hashes, all RNG streams, seed splits and requested/actual clocks.
2. Keep one training executor, unique run directories and a portable resource/incident receipt. Refuse source/protocol drift mid-arm; never auto-extend a run based on scores.
3. Provide smoke, single-seed shakedown and five-seed fixed-budget modes. Save initial/final checkpoints and per-update/event telemetry; evaluate only after training exits.
4. Do not edit pqn_sampler_diagnostic.py, its tests or archived protocols/results. They remain historical reproduction assets.

## Acceptance evidence

- Fake tiny trainer/evaluator tests prove exact arm cardinality, seed isolation, resource-stop behavior, checkpoint lineage and no promotion side effects.
- Failed arms remain in results with their cause; no silent retry or drop. Initial/final pairing is by independent training seed, not by rows or worlds.

Focused target files below include new files this package creates. Commands and flags described in the steps are implementation requirements, not claims that they already exist.

```bash
SNAKE_DQN_DEVICE=cpu OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  /Users/josenunez/Projects/ml/snake-dqn/venv/bin/python -m pytest -q \
  tests/test_pqn_correctness_diagnostic.py
```

## Compatibility and rollback

New tool only; deleting/reverting it does not alter the prior campaign or model behavior.

## Handoff

Write command/result logs, source/config/checkpoint hashes, changed files, commit SHA, unresolved risks and interface changes to `<EVIDENCE_ROOT>/packages/h0/`. The root resolves EVIDENCE_ROOT to an absolute durable path outside disposable worktrees before dispatch. Obtain a root-issued test/compute slot before running commands. Return absolute artifact paths and hashes; root verifies the durable manifest before accepting the dependency or cleaning a worktree. A passing narrow test does not waive an upstream gate or promote a model.

Audit source: [expanded state/system review](../../../state_and_system_review_2026-09-06.md).
