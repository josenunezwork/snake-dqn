# Apex teacher-path local geometry

The independent audit passed. The fixed branch is `LOCAL_PROGRESS_GAP_REMAINS`: five of six seed/bank cells exceeded both 95% pooled and macro-case local-equivalence thresholds; seed 2026099201 on the formerly fresh adaptive bank did not (92.43% pooled, 92.36% macro).

Most exact teacher disagreements are different safe moves with the same next-head Manhattan distance. Counting these alternatives raises agreement to 95.66–97.21% in the other five cells. [Every seed and both banks](table.md) report exact, equal-cost, and strictly longer choices. The shared 48 teacher cases in each bank are reused across models, not independent teacher replications.

The diagnostic read saved records and already audited greedy decisions on 6,153 primary slots (pre-action food count one, including the second-food-eating frame). It validated all 6,144 teacher frames, direction chains, normal movement, masks, food counters, teacher argmin and deterministic tie breaks. No model/checkpoint loads, forwards, new gameplay, frames or optimization occurred. Independent aggregation audited all slots, 288 case summaries and six cells.

Local Manhattan progress is not long-horizon optimality: an equal-cost move can lead somewhere different, and a longer move can avoid a later trap. This result does not prove a capacity, representation, TD or reward defect. Existing gameplay failures and retention gates remain unchanged. No promotion occurred.

The original root pre-audit admission check used the wrong supervisor receipt field (`cause` instead of `termination`). It stopped before any audit admission or numerical work. The correction is preserved in `audit-pre-admission-checker-correction.json`; the analysis and the audit each ran once. There was no scientific retry or recovery.

Both supervised numerical jobs ended naturally in 1.2554671249818057 seconds combined. Peak child 157220864 was 157220864 bytes; available memory stayed above 34500444160 bytes. The study is sealed and its window is closed. There is no new learning curve because no updates occurred; prior gameplay and learning curves remain in the [second-food report](../apex_second_food_continuation_2026-09-22/README.md).

The user separately authorized a fresh-start scaling comparison: current versus larger network, narrow versus broader starting-world sets, equal data/update dose and three seeds. That study is being implemented and is not yet admitted. It will use actual greedy gameplay as the primary outcome.

Frozen identities: intent `385072df82eb0b6e9bc16c80d5d98782e4e115eaeb4fccc7e2027b6642add816`; analysis `69a7198db34aabbc61ee7c3ea162266cfa4cb55ca083b5d003df8ef793ffab7a`; audit `05f6cbad92e7be6127cbc1a52dfd43fbbe8a488945e6f623294c2e1f394d55d6`; closeout `77a2748b9cb74ab85c9c6a042451085cbf81f108b56c654f98c2a95a8170616b`.
