# A0 — Version distributed replay-slot identities

**Status:** PLANNED. **Earliest wave:** 0. **Role:** builder. **Kind:** implementation.

**Dependencies:** None; can start in the first parallel group.

Prevent sampled TD errors from reprioritizing a different transition after slot reuse.

Read [decisions and wave plan](../README.md), [agent runbook](../agent_runbook.md), and [dispatch.json](../dispatch.json) before starting. You are not alone: edit only this package's claimed files and inherit accepted dependency commits.

## Exclusive write ownership

- `src/training/sum_tree.py`
- `src/training/apex_buffer.py`
- `src/training/apex_learner.py`
- `tests/test_sum_tree.py`
- `tests/test_apex_buffer.py`
- `tests/test_apex_learner.py`
- `tests/test_apex_replay_identity.py` — new

## Implementation / execution steps

1. Add monotonically increasing slot generations and versioned sample/update handles. Preserve the synchronous local replay API.
2. Carry (slot,generation) through buffer, learner and priority IPC without losing alignment. Reject stale generations; count accepted/stale/malformed updates.
3. Keep alpha/beta schedules, n-step returns, Q targets and mask interpretation unchanged in this package. Bound message processing only as needed to guarantee priority/sample service; measure rather than retune throughput.

## Acceptance evidence

- Capacity-two overwrite fixture rejects stale updates and accepts current generations. Duplicate indices and partial stale batches preserve index/error alignment.
- End-to-end buffer-client handle round trip passes. Old index-only distributed IPC fails clearly; local replay tests remain green.
- No persistent replay migration is assumed: document the current lack of distributed replay restoration.

Focused target files below include new files this package creates. Commands and flags described in the steps are implementation requirements, not claims that they already exist.

```bash
SNAKE_DQN_DEVICE=cpu OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  /Users/josenunez/Projects/ml/snake-dqn/venv/bin/python -m pytest -q \
  tests/test_apex_replay_identity.py \
  tests/test_sum_tree.py \
  tests/test_apex_buffer.py \
  tests/test_apex_learner.py
```

## Compatibility and rollback

Upgrade actor/learner/buffer code as one deployment unit. Keep stale counters and no silent legacy-message fallback.

## Handoff

Write command/result logs, source/config/checkpoint hashes, changed files, commit SHA, unresolved risks and interface changes to `<EVIDENCE_ROOT>/packages/a0/`. The root resolves EVIDENCE_ROOT to an absolute durable path outside disposable worktrees before dispatch. Obtain a root-issued test/compute slot before running commands. Return absolute artifact paths and hashes; root verifies the durable manifest before accepting the dependency or cleaning a worktree. A passing narrow test does not waive an upstream gate or promote a model.

Audit source: [expanded state/system review](../../../state_and_system_review_2026-09-06.md).
