# Core dependencies for Snake DQN Game
torch>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0
psutil>=5.9.0,<6.0.0

# Web UI backend (the desktop PyQt5 GUI was retired; see web/). Also in
# web/requirements.txt — kept here so the default install can serve the app.
fastapi>=0.110
uvicorn[standard]>=0.27
websockets>=12

# Database
# SQLite is built into Python, no external dependency needed

# For future ML tracking
tensorboard>=2.14.0,<3.0.0

# Configuration management
pyyaml>=6.0.1,<7.0.0
pydantic>=2.0.0,<3.0.0

# Runtime utilities
matplotlib
tqdm

