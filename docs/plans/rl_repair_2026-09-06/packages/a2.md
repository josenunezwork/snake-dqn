# A2 — Record Apex recipes and apply explicit new mask semantics

**Status:** PLANNED. **Earliest wave:** 3. **Role:** builder. **Kind:** implementation.

**Dependencies:** C0, A0, A1, OFF, S1

Give local/distributed Apex explicit seed, recipe, target-mask and continuation provenance.

Read [decisions and wave plan](../README.md), [agent runbook](../agent_runbook.md), and [dispatch.json](../dispatch.json) before starting. You are not alone: edit only this package's claimed files and inherit accepted dependency commits.

## Exclusive write ownership

- `src/training/base_dqn_policy.py`
- `src/training/apex_policy.py`
- `src/training/apex_actor.py`
- `src/training/apex_buffer.py`
- `src/training/apex_learner.py`
- `src/training/replay_buffer.py`
- `src/training/multistep_buffer.py`
- `src/training/td_targets.py`
- `src/scripts/apex_train.py`
- `src/main.py`
- `tests/test_apex_policy.py`
- `tests/test_apex_actor.py`
- `tests/test_apex_buffer.py`
- `tests/test_apex_learner.py`
- `tests/test_apex_train_config.py`
- `tests/test_replay_quality_pure.py`
- `tests/test_multistep_buffer.py`
- `tests/test_td_target_helpers.py`
- `tests/test_main_cli_guard.py`
- `tests/test_main_training_state.py`
- `src/training/apex_recipe.py` — new
- `tests/test_apex_recipe.py` — new
- `tests/test_apex_mask_roundtrip.py` — new

## Implementation / execution steps

1. Use C0 named seeds before construction. Record distinct local-AdamW/clip 50 and distributed-Adam/clip 100 recipes and beta clock meanings; do not unify numerical recipes in this repair.
2. Carry the selected action-mask contract through collection, n-step, replay and TD targets. New mask-mode rows have known semantics; legacy advisory masks are never relabeled as hard legality. Preserve incumbent serving behavior.
3. Reject unsupported distributed curriculum before spawning; local curriculum stays available. Do not reopen central config files: consume C0 fields or request a separate C0 handoff.
4. Validate optimizer/recipe conflicts. Distinguish weights-only fine-tune from optimizer/odometer continuation. Distributed Apex lacks replay/in-flight actor state, so do not promise bit-exact resume without a separately implemented full-state protocol.
5. Retain A0 generation tokens and A1 liveness/budget behavior. Count policy-version age, fallback-mask rows and effective data/update clocks.

## Acceptance evidence

- Same explicit seed repeats initialization and a controlled single-actor short trace; asynchronous multi-actor schedule is not claimed bit deterministic.
- Mask semantics survive n-step/replay and mixed legacy presence cases. Bootstrap uses gamma**actual_n and actual done; malformed width/version fails.
- Conflicting recipe resumes fail; unsupported curriculum fails before child creation. All A0 stale-handle and A1 failure cleanup cases remain green.

Focused target files below include new files this package creates. Commands and flags described in the steps are implementation requirements, not claims that they already exist.

```bash
SNAKE_DQN_DEVICE=cpu OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  /Users/josenunez/Projects/ml/snake-dqn/venv/bin/python -m pytest -q \
  tests/test_apex_recipe.py \
  tests/test_apex_mask_roundtrip.py \
  tests/test_apex_policy.py \
  tests/test_apex_actor.py \
  tests/test_apex_buffer.py \
  tests/test_apex_learner.py \
  tests/test_td_target_helpers.py \
  tests/test_multistep_buffer.py \
  tests/test_apex_runtime.py
```

## Compatibility and rollback

Existing checkpoints remain evaluable; legacy continuation is explicit/unverified. Revert the new recipe selection without removing prior replay/liveness repairs.

## Handoff

Write command/result logs, source/config/checkpoint hashes, changed files, commit SHA, unresolved risks and interface changes to `<EVIDENCE_ROOT>/packages/a2/`. The root resolves EVIDENCE_ROOT to an absolute durable path outside disposable worktrees before dispatch. Obtain a root-issued test/compute slot before running commands. Return absolute artifact paths and hashes; root verifies the durable manifest before accepting the dependency or cleaning a worktree. A passing narrow test does not waive an upstream gate or promote a model.

Audit source: [expanded state/system review](../../../state_and_system_review_2026-09-06.md).
