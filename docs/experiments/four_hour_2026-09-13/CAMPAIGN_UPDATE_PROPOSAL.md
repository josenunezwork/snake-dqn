# Superseded campaign update draft

The earlier draft in this path predates completed padding, off-policy behavior,
capacity reproduction, the full eight-cell capacity evaluation, and selection
of the active learning curve. It must not be applied to `campaign.json`.

Use [EXPERIMENT_LEDGER.md](EXPERIMENT_LEDGER.md), terminal receipts, and the
root-owned `campaign.json` for the current state. A new campaign update should
be generated only after the active learning-curve seed-1 arm reaches terminal
closure and its next action is known.
